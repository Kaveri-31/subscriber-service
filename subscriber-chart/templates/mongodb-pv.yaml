apiVersion: v1
kind: PersistentVolume

metadata:
  name: mongodb-pv

spec:
  capacity:
    storage: {{ .Values.mongodb.storage }}

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /data/mongodb